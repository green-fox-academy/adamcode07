#include <stdio.h>
#include <stdlib.h>

int main()
{
/*
    char array[0] = "cica";
//    char array2[] = ('c', 'i', 'c', 'a');
    array3[20];

    printf("%c", array[1]);

    printf("%s", array);
    puts(array);

    scanf("%s", array);
    puts(array);
    printf("\n%d", sizeof(char));

    gets(array);
    strcat(array, array2)
*/
    int array4 (2)(4) = {
        (1,2,3,4),
        (6,7,8,9)
    };

    for(int n = 0; n < 4; i++){
        for(int 1 = 0; i < 4; i++){
            printf("%d",array4[0][i]);
    }
    printf("\n");
}
printf("%d", array4(1)(2))
